import { OpenDot } from "@/components/calendar/MonthGrid";

export default function MonthGridOpenDot() {
  return (
    <span className="flex items-center gap-1.5 text-sm">
      <OpenDot /> 근무 중
    </span>
  );
}
