export const metadata = { title: "Card" };

# Card

흰 바탕 둥근 상자. 화면 본문을 묶는 기본 단위.

<ComponentPreview name="card/basic" width="max-w-md" />

<FileLocation path="web/src/components/ui.tsx" importLine={`import { Card } from "@/components/ui";`} />

## 사용법

```tsx
<Card>
  <p className="text-sm text-muted">이번 주 근무</p>
  <p className="mt-1 text-2xl font-bold tabular-nums">18시간 30분</p>
</Card>
```

## 구성

```
Card  <section rounded-2xl border border-line bg-surface p-5 {className}>
└─ children
```

## 여백 덮기

`className` 으로 안쪽 여백을 바꾼다. 달력 격자가 쓰는 방식이다.

<ComponentPreview name="card/padding" width="max-w-md" />

## 하지 말 것

- 카드 안에 카드를 넣지 않는다.
- `className` 으로 배경·테두리 색을 바꾸지 않는다 — 여백·간격 조정에만 쓴다.

## API 레퍼런스

<PropsTable
  rows={[
    { prop: "children", type: "React.ReactNode", required: true },
    { prop: "className", type: "string", default: '""', description: "뒤에 덧붙는다" },
  ]}
/>
