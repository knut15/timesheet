export const metadata = { title: "Icon Button" };

# Icon Button

헤더 오른쪽에 두는 아이콘만 있는 둥근 버튼. 이름은 `aria-label` 과 `title` 로 붙는다.

<ComponentPreview name="icon-button/basic" />

<FileLocation path="web/src/components/shell.tsx" importLine={`import { IconButton } from "@/components/shell";`} />

## 사용법

```tsx
<IconButton icon={LogOut} label="로그아웃" onClick={() => logout()} />
```

## 구성

```
IconButton  <button type="button" aria-label={label} title={label}, 40×40 rounded-full>
└─ Icon 20px aria-hidden
```

## 헤더 안

`AppHeader` 의 `actions` 자리. App Header 의 "마스터 + 로그아웃" 과 같은 데이터다.

<ComponentPreview name="icon-button/in-header" fixed />

## 사용 규칙

<DocLink path=".claude/skills/timesheet-ui/SKILL.md" hash="아이콘은-lucide-react-만">timesheet-ui 스킬 §2 아이콘</DocLink> 마지막 줄.

## 하지 말 것

- 아이콘 버튼을 `button` 안에 아이콘만 넣어 직접 만들지 않는다.
- `label` 을 비우지 않는다.

## API 레퍼런스

<PropsTable
  rows={[
    { prop: "icon", type: "LucideIcon", required: true },
    { prop: "label", type: "string", required: true, description: "`aria-label`·`title`" },
    { prop: "onClick", type: "() => void", required: true },
  ]}
/>
