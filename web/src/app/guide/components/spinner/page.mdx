export const metadata = { title: "Spinner" };

# Spinner

불러오는 중 자리 표시. 회전 그림 없이 글자 `불러오는 중…`. 미리보기는 높이 240px 틀 안이다.

<ComponentPreview name="spinner/basic" width="max-w-md" />

<FileLocation path="web/src/components/ui.tsx" importLine={`import { Spinner } from "@/components/ui";`} />

## 사용법

```tsx
if (!data) return <Spinner />;
```

## 구성

```
Spinner  <div flex-1 가운데 py-20 text-sm text-muted>
└─ "불러오는 중…"
```

## 하지 말 것

- **앱 화면에서 쓰지 않는다.** 2026-09-25 부터 불러오는 중은 전부 [Skeleton](/guide/components/skeleton) — 불러온 뒤와 같은 크기라 화면이 움직이지 않는다.

## API 레퍼런스

props 없음.
