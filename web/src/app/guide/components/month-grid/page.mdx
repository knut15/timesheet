export const metadata = { title: "Month Grid" };

# Month Grid

월요일 시작 월 격자. 날짜 칸은 버튼이고, 칸 내용은 부르는 쪽이 `cell` 로 채운다. 멤버·마스터 달력이 같이 쓴다.

<ComponentPreview name="month-grid/basic" width="max-w-md" />

<FileLocation path="web/src/components/calendar/MonthGrid.tsx" importLine={`import { MonthGrid, OpenDot, type Marks } from "@/components/calendar/MonthGrid";`} />

## 사용법

```tsx
const [selected, setSelected] = useState<string | null>("2026-09-25");
<MonthGrid
  year={2026}
  month={8}
  todayKey="2026-09-25"
  selectedKey={selected}
  onSelect={setSelected}
  cellHeight={56}
  cell={(key) => ({ label: dateLabel(key), marks: {}, body: null })}
/>
```

`dateLabel`·`dayAriaLabel`·`compactHours` 는 `@/lib/calendar`. 실제 화면은 `dayAriaLabel` 로 `label` 을 만든다.

## 구성

```
MonthGrid  Card className="px-2 py-3"
├─ 격자  <div role="group" aria-label="2026년 9월 달력" aria-busy={busy}>
│  ├─ 요일 줄  <div aria-hidden grid-cols-7> 월 화 수 목 금 토 일
│  └─ 칸 줄  <div grid-cols-7>
│     ├─ 이번 달 밖  <div aria-hidden opacity-40> 숫자만
│     └─ 날짜 칸  <button aria-label={label} aria-pressed={selected} aria-current="date"(오늘)>
│        ├─ 1줄
│        │  ├─ 날짜 숫자 (오늘이면 ring-accent 원, 선택이면 굵게)
│        │  └─ 표시 자리 aria-hidden: OpenDot(open="today") · TriangleAlert(open="stale") · Hourglass(pending)
│        └─ 본문 aria-hidden: cell(key).body (busy 면 비움)
└─ footer  <div mt-2> (있을 때)
테두리: 점선 marks.dashed 또는 투명. 선택은 테두리 없이 bg-accent/10 + 날짜 숫자 font-bold (점선은 선택돼도 유지)
포커스: focus-visible:outline-2 outline-offset-1 outline-accent — 키보드 이동 때만 보이는 바깥 선
```

`OpenDot` — 근무 중 표시. 칸·범례·상세가 같은 모양을 쓴다.

<ComponentPreview name="month-grid/open-dot" />

아래 예시는 모두 2026년 9월, 오늘은 `2026-09-25` 로 고정한다. 멤버·마스터 칸 데이터는 이 파일 하나를 같이 쓴다.

<SourceCode name="month-grid/demo-data" />

## 멤버 달력

시간·휴가 글자·상태 표시와 범례. 읽는 이름은 `dayAriaLabel` 이 만든다 — 예: `9월 23일 수요일, 9시간 근무, 퇴근 기록 없음, 수정 요청 대기 중`.

<ComponentPreview name="month-grid/member" width="max-w-md" />

## 마스터 달력

아바타 겹침과 시간. `cellHeight={64}`.

<ComponentPreview name="month-grid/master" width="max-w-3xl" />

## 불러오는 중

`busy` 면 칸 내용·표시 대신 작은 막대를 두고 `aria-busy` 를 켠다(스크린리더용 `불러오는 중` 한 줄). 칸 높이는 그대로라 격자는 움직이지 않는다. 범례는 데이터와 무관하므로 `footer` 에 그대로 둔다 — `불러오는 중…` 글자는 두지 않는다.

<ComponentPreview name="month-grid/busy" width="max-w-md" />

## 선택과 점선

점선 칸(9월 24일 유급 휴가)을 선택해도 점선은 남고, 연한 배경과 굵은 날짜가 더해진다. 파란 테두리는 없다.

<ComponentPreview name="month-grid/selected-dashed" width="max-w-md" />

## 키보드 포커스

Tab 으로 칸을 옮겨 보세요. 키보드로 칸에 가면 바깥 선(accent)이 보이고, 마우스·터치 선택에는 없다.

<ComponentPreview name="month-grid/keyboard" width="max-w-md" />

## 6줄 달

줄 수는 달마다 4~6이다. 2026년 11월은 6줄, 오늘은 이 달 밖이다.

<ComponentPreview name="month-grid/six-weeks" width="max-w-md" />

## 사용 규칙

<DocLink path="docs/design/calendar.md" hash="3-월-격자">달력 명세 §3 월 격자</DocLink>(치수·칸 배치·상태 표), <DocLink path="docs/design/calendar.md" hash="7-접근성">§7 접근성</DocLink>, <DocLink path=".claude/skills/timesheet-ui/SKILL.md" hash="4-달력">timesheet-ui 스킬 §4 달력</DocLink>.

## 하지 말 것

- `body` 에 긴 글자(`4시간 30분`)를 넣지 않는다 — `compactHours` 로 줄이고 정확한 값은 `label` 에 둔다.
- `label` 을 날짜만으로 두지 않는다 — 칸 내용이 `aria-hidden` 이라 `label` 이 유일한 읽는 글자다.
- 오늘 칸을 accent 로 채우지 않는다 (<DocLink path="docs/design/calendar.md" hash="8-다크-모드--새-토큰-없음">달력 명세 §8</DocLink>).
- 선택 칸에 테두리(`border-accent` 등)를 다시 넣지 않는다. 포커스 바깥 선(`focus-visible`)을 지우지 않는다.

## API 레퍼런스

### MonthGrid

<PropsTable
  rows={[
    { prop: "year", type: "number", required: true },
    { prop: "month", type: "number", required: true, description: "0~11" },
    { prop: "todayKey", type: "string", required: true, description: "`YYYY-MM-DD` (`dayKey`)" },
    { prop: "selectedKey", type: "string | null", required: true, description: "선택한 날" },
    { prop: "onSelect", type: "(key: string) => void", required: true, description: "칸을 누르면 그 날 키" },
    { prop: "busy", type: "boolean", default: "false", description: "`aria-busy`, 칸 내용·표시 숨김" },
    { prop: "cellHeight", type: "56 | 64", required: true, description: "멤버 56, 마스터 64 (버튼 높이 52 / 60)" },
    { prop: "cell", type: "(key: string) => { label: string; marks: Marks; body: React.ReactNode }", required: true, description: "이번 달 날짜마다 불린다" },
    { prop: "footer", type: "React.ReactNode", description: "격자 아래 (범례·빈 달 문구)" },
  ]}
/>

### Marks

<PropsTable
  first="필드"
  rows={[
    { prop: "open", type: '"today" | "stale"', description: "근무 중 점 / 퇴근 기록 없음 아이콘" },
    { prop: "pending", type: "boolean", description: "요청 대기 아이콘" },
    { prop: "dashed", type: "boolean", description: "점선 테두리 (휴가·대타)" },
  ]}
/>

### OpenDot

props 없음. 6px 초록 점, `aria-hidden`.
