export const metadata = { title: "Field" };

# Field

입력칸 이름표. 이름 글자 아래에 입력 요소를 둔다. 입력 요소 자체는 CSS 클래스 `.field` 로 꾸민다.

<ComponentPreview name="field/basic" width="max-w-md" />

<FileLocation path="web/src/components/ui.tsx · web/src/app/globals.css (@layer components 의 .field)" importLine={`import { Field } from "@/components/ui";`} />

## 사용법

```tsx
<Field label="닉네임">
  <input className="field" defaultValue="김하늘" />
</Field>
```

## 구성

```
Field  <label block text-sm>
├─ <span text-muted>{label}</span>
└─ <div mt-1>{children}</div>
     └─ (넘긴 입력 요소, 보통 className="field")
.field = w-full rounded-xl border border-line bg-background px-3 py-2.5 text-base, 포커스 시 border-accent
```

## 날짜

`type="date"` 두 칸.

<ComponentPreview name="field/date" width="max-w-md" />

## 선택

`select` 에도 `className="field"`.

<ComponentPreview name="field/select" width="max-w-md" />

## 오류와 함께

아래에 `ErrorText`.

<ComponentPreview name="field/with-error" width="max-w-md" />

## 하지 말 것

- `Field` 안에 입력 요소를 두 개 넣지 않는다 — `label` 하나가 입력 하나를 가리킨다.
- 입력칸 글자를 16px 아래로 줄이지 않는다(`.field` 는 `text-base`).

## API 레퍼런스

<PropsTable
  rows={[
    { prop: "label", type: "string", required: true, description: "이름 글자" },
    { prop: "children", type: "React.ReactNode", required: true, description: "입력 요소" },
  ]}
/>
