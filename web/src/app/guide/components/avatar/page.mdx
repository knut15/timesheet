export const metadata = { title: "Avatar" };

# Avatar

멤버 아바타. 사진 없이 이름 이니셜과 사용자 id 로 고정된 배경색.

<ComponentPreview name="avatar/basic" />

<FileLocation path="web/src/components/shell.tsx" importLine={`import { Avatar, initials } from "@/components/shell";`} />

## 사용법

```tsx
<Avatar name="김하늘" seed="user-demo-01" />
```

## 구성

```
Avatar  <span rounded-full, 흰 글자, 배경 = AVATAR_COLORS[hash(seed) % 8]>
└─ initials(name)
   role="img" + aria-label (label 있을 때) / aria-hidden (없을 때)
```

## 한글·로마자 이니셜

한글은 첫 글자, 로마자는 두 글자 대문자, 빈 이름은 `?`. 각 아바타 아래 글자는 `initials()` 의 실제 결과다.

<ComponentPreview name="avatar/initials" />

## Sizes

xs 18 · sm 32 · md 40 · lg 64.

<ComponentPreview name="avatar/sizes" />

## 색 고정

같은 seed 는 이름이 달라도 같은 색이다(윗줄). `user-demo-01` ~ `user-demo-08` 여덟 개에 8색이 하나씩 나온다(아랫줄).

<ComponentPreview name="avatar/fixed-color" />

## 접근성

왼쪽은 이름 글자 옆 장식이라 `aria-hidden`, 오른쪽은 혼자 쓰므로 `label` 을 준다.

<ComponentPreview name="avatar/a11y" />

## 사용 규칙

<DocLink path=".claude/skills/timesheet-ui/SKILL.md" hash="3-멤버-아바타--avatar">timesheet-ui 스킬 §3 멤버 아바타</DocLink> — seed = 사용자 id, 크기별 쓰는 곳, 이름 옆 배치.

## 하지 말 것

- `seed` 에 이름·닉네임을 넣지 않는다.
- `className`·`style` 로 색·크기를 덮지 않는다 — props 에 없다. 새 크기가 필요하면 `SIZES` 에 추가를 요청한다.
- 사진 `img` 를 섞지 않는다 (사진 업로드 기능이 없다).

## API 레퍼런스

### Avatar

<PropsTable
  rows={[
    { prop: "name", type: "string", required: true, description: "이니셜의 원본" },
    { prop: "seed", type: "string", required: true, description: "색을 정하는 값. 사용자 id" },
    { prop: "size", type: '"xs" | "sm" | "md" | "lg"', default: '"md"', description: "18 / 32 / 40 / 64px, 글자 9px / 12 / 14 / 20" },
    { prop: "label", type: "string", description: "있으면 `role=\"img\"` + `aria-label`, 없으면 `aria-hidden`" },
  ]}
/>

### initials(name: string): string

<SimpleTable
  head={["입력", "출력"]}
  rows={[
    ["빈 문자열·공백", "`\"?\"`"],
    ["로마자로 시작, 두 단어 이상", "앞 두 단어 첫 글자, 대문자"],
    ["로마자로 시작, 한 단어", "앞 두 글자, 대문자"],
    ["그 밖(한글 등)", "첫 글자"],
  ]}
/>
