# 2026-09-25

> 사후 기록이다. `work-history` 규칙이 이 작업이 끝난 뒤에 생겼다. 시각은 커밋·배포 로그로 확인된 것만 적었고 나머지는 순서만 맞다.
> 읽기 좋게 정리한 글: 볼트 `studio-curvez/timesheet/` 시리즈 "Timesheet 개발기".

## 요청 1
> timesheet 에 claude.md 작성한다. 모든 문서는 docs에 기록된다. prd — 파트타임 출/퇴근 시간과 급여계산. 급여는 시급 + 주휴수당(공식 페이지 확인). 오버타임 계산. 매장 근처 50M 안에 들어가면 출근도장을 찍는 알림이 뜨고, 출근체크가 안되어 있으면 알림이 발송된지 1분 후 다시 재알림. 위 내용은 각 문서로 만들고 링크를 걸어준다. MVP 배포까지 한 후 web url주소 알려줘.

### [결정] 1차 MVP 는 웹만, 저장은 localStorage
- 기존 뼈대(server: Express+Prisma, web: Next 16)가 있었지만 서버 DB 호스팅을 새로 만들지 않고 웹만 배포
- 이유: 외부 DB 생성은 승인 사항, 요청은 MVP 배포까지

### [결정] 급여 규칙 출처
- 최저임금 10,320원 — 최저임금위원회 결정현황
- 주휴 요건·계산식 — 생활법령정보, 고용노동부 1350
- 단시간근로자 초과근로 가산 — 기간제법 제6조
- 5인 미만 가산 제외 조문 원문 — 확인 불가로 PRD 에 표기

### [검증] 급여·알림 단위 테스트
- `pnpm --filter timesheet-web test` → 15 tests 15 passed

### [실패] 첫 웹 배포 — prebuilt 업로드
- 증상: `Error: Please ensure project dependencies have been installed: File does not exist: "node_modules/.pnpm/@swc+helpers@0.5.23/..."`
- 원인: `web/` 에서 배포. pnpm 은 패키지를 워크스페이스 루트 `node_modules/.pnpm` 에 두므로 업로드 범위 밖
- 수정: 저장소 루트에서 `vercel build` + `deploy --prebuilt`, `rootDirectory: web`
- 확인: `curl` `/` 200, `/sw.js` 200

### [수정] 루트 .gitignore 에 .vercel 누락
- `grep -n '^.vercel\|^\.env' .gitignore || printf ...` — `.env` 가 있어 grep 성공, `.vercel` 이 추가되지 않음. `git status` 에서 발견해 따로 추가

### [검증] 배포본 브라우저 확인 (가짜 GPS·알림)
- 60m 무알림, 33m 진입 즉시 첫 알림, 60.004초 재알림 1회, 84초까지 추가 없음, "지금 출근" 후 알림 멈춤
- 도구 실패: `Runtime.evaluate timed out after 45000ms` — 스크립트 하나로 66초 대기한 탓. 나눠서 읽음

### [배포] 웹 https://timesheet-brown-ten.vercel.app (dpl_Fvg4SEmrXK2TFbKKb7qhTtz3iDvw)

## 요청 2
> PRD 추가. 로그인 구현. cms에 관리자 구현. 마스터는 로그인 후 멤버(알바생)을 초대할 수 있다. 초대코드를 받은 멤버는 로그인(가입) 후 초대 코드를 등록하여 해당 매장에서 관리된다. 마스터 화면은 대시보드를 포함한다.

### [결정] 사용자에게 물어 정함
- 관리자 위치: timesheet 안의 관리 화면 (`~/Workspace/cms` 아님)
- 범위: PRD + 구현 + 배포
- 로그인: 직접 구현(JWT, web-auth 스킬)
- 패키지: 전부 설치 (argon2, jose, cookie-parser, helmet, express-rate-limit, zod-to-openapi, swagger-ui-express, openapi-fetch, openapi-typescript)
- 인프라: Neon + Vercel

### [실패] pnpm 설치 — 빌드 스크립트
- 증상: `ERR_PNPM_IGNORED_BUILDS: Ignored build scripts: @scarf/scarf@1.4.0`, 이어서 `duplicated mapping key (20:3)`
- 원인: pnpm 11 은 빌드 스크립트 명시 허용 필요. `pnpm add` 가 자리표시 줄을 남겨 키 중복
- 수정: `allowBuilds` 에 `argon2: true`, `"@scarf/scarf": false`, 자리표시 줄 삭제

### [실패] Prisma migrate reset 차단
- 증상: `If the user explicitly consents, you may rerun this command with the PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION ...`
- 원인: 내가 먼저 `prisma/migrations` 를 지우고 reset 을 시도. Prisma 가 AI 의 DB 초기화를 막음
- 수정: 우회하지 않음. init 마이그레이션을 체크섬(`1c4ad119…`)까지 같게 재생성하고 FK 를 두 번째 마이그레이션으로
- 교훈: 마이그레이션 폴더를 지우지 않는다

### [실패] e2e 8/10 실패 — 403
- 원인: 테스트 클라이언트가 쿠키 헤더를 만든 뒤에 CSRF 토큰을 받음. 서버는 옳았다
- 수정: 토큰을 먼저 받는다 → 10/10, S23 추가 후 11/11

### [실패] 웹 타입 — membership possibly null (11곳)
- 원인: `MyMembershipDto.nullable()` 을 zod-to-openapi 가 참조 대상 스키마 자체를 nullable 로 생성
- 수정: `z.union([MyMembershipDto, z.null()])`

### [실패] 웹 타입 — navigator.locks
- 증상: `Type 'Promise<boolean | Promise<boolean>>' is not assignable ...`, 단언 시 `TS2352`
- 수정: `.then((ok) => ok)`

### [실패] lint — `useCallback(load, deps)`
- 증상: `Expected the dependency list for useCallback to be an array literal`
- 수정: 의존성을 문자열 key 하나로

### [검증] 로컬
- server e2e 11/11, S17(세션 상한 5초) 1/1, typecheck·lint 0, web build 10 routes

### [결정] Neon 약관
- `integration_terms_acceptance_required` — 사용자가 브라우저에서 동의 후 재실행

### [배포] DB 마이그레이션 — Neon `ep-bitter-cloud-aw1918ms` 에 2건 적용

### [실패] API 배포 1 — `fetch failed`
- 업로드 중 일시 오류. 재시도로 빌드까지 진행

### [실패] API 배포 2·3 — 원격 빌드만 TS2349 (helmet)
- 증상: `src/app.ts(16,9): error TS2349: This expression is not callable. Type 'typeof import(".../helmet/index")' has no call signatures.`
- 원인: 미확인. 로컬에서 module/moduleResolution/esModuleInterop/type 조합 6가지로 재현 실패. Vercel 빌더의 타입 검사가 helmet 기본 내보내기를 모듈 객체로 봄
- 1차 수정 `import * as helmet` + `helmet.default()` → 같은 에러(18,16)
- 2차 수정: 기본 import 유지 + `as unknown as () => RequestHandler` 타입 단언 (런타임은 index.cjs 가 `module.exports.default = module.exports`)

### [실패] API 배포 4 — 모든 요청 500
- 증상: `FUNCTION_INVOCATION_FAILED`, 로그 `Invalid export found in module "/var/task/server/src/app.js". The default export must be a function or server.`
- 원인: Vercel Express 진입점이 src/app.ts 를 server.ts 보다 먼저 고름 (vercel logs 로 확인)
- 수정: app.ts `export default app`
- 확인: /health 200, /api/users/me 401, /api/docs 404

### [배포] API https://timesheet-api-hazel.vercel.app

### [실패] 웹 재배포 1 — `No Next.js version detected`
- 원인: `vercel pull` 이 서버 쪽 설정(rootDirectory null)으로 로컬 project.json 을 덮어씀
- 수정: `vercel api /v9/projects/timesheet` PATCH `rootDirectory: web`

### [실패] 웹 재배포 2 — `Invalid rewrite found`
- 증상: `destination does not start with /, http://, or https:// ... "[SENSITIVE]/api/:path*"`
- 원인: `API_URL` 을 Sensitive 로 넣어 pull 이 값을 가림
- 수정: `vercel env add API_URL production --no-sensitive`
- 뒤이어 `fetch failed` 1회 → 재시도

### [수정] 루트 package.json 에 `"vercel-build": "next build"` 가 생김
- 실패한 vercel build 의 부산물. `git checkout -- package.json`

### [검증] 운영 e2e
- 9/11 → S5 `SyntaxError: Unexpected token 'F', "Forbidden"`: Vercel 방화벽이 alg:none 을 403 (`x-vercel-mitigated: deny`). 테스트가 플랫폼 거부를 인정하도록 수정 후 통과
- I-1 `429`: Vercel 이 X-Forwarded-For 를 덮어써 전부 실제 IP 한 개로 잡힘(`refresh_tokens.ip` 조회). S4·S19 실패가 창을 채움. 성공 로그인은 카운트 안 됨 확인(`r=4` 유지). 1분 쉬고 단독 실행 → 통과
- `--test-name-pattern` 을 파일 뒤에 두면 필터 안 됨
- 테스트 데이터 삭제: 사용자 24, 매장 2 → 운영 DB 사용자 0

### [검증] 브라우저 — 비로그인 /admin → /login 확인. 390px 폭·로그인 화면은 미검증

### [커밋] 13:33~13:34 `008103b` `0c7a807` `2379684` `caf1103` (feature/auth-admin), push, PR https://github.com/knut15/timesheet/pull/1 — Vercel 체크 2건 pass

## 요청 3 이후
- 스킬 추가: `vercel-monorepo-deploy`, `kr-parttime-pay`, `work-history`, web-auth `references/express.md`
- 볼트 정리: `studio-curvez/timesheet/` 9편

## 요청 4 (13:54)
> 헤더와 푸터, 멤버에 대한 디자인을 적용해줘. 멤버는 아바타도 만들어주bottom nav에는 lucide 설치해서 아이콘도 넣어주고. 위 내용을 스킬로 만들고, 내가 계속 지시하지 않아도 바뀐부분은 업데이트 하도록 수정해줘.

### [결정] 따를 시안이 없다
- 아티팩트 목록·저장소(.curvez/design 없음)에 timesheet 시안 없음 → 기존 색 토큰(globals.css) 위에서 헤더·하단 내비·아바타를 새로 정한다
- "푸터" = 하단 내비게이션으로 해석 (멤버·마스터 공통)

### [결정] 13:59 디자인 셸 — shell.tsx 한 파일
- AppHeader(매장·제목·내 아바타), BottomNav(푸터, lucide 아이콘), Avatar(이니셜 + 사용자 id 해시 색 8종)
- 마스터 화면 상단 탭 → 하단 내비로 통일
- lucide-react 1.47.0 설치 (사용자 지시)

### [수정] 초대 사용자 아바타 색 불일치
- InviteDto 에 사용자 id 가 없어 이름으로 색을 정하는 우회 주석을 달았다가, 계약에 `usedByUserId` 를 추가하는 쪽으로 바꿈
- 확인: openapi 재생성, web 타입 생성, 로컬 e2e 11/11 (usedByUserId 단정 추가)

### [검증] 13:59 web typecheck·lint 0, 단위 15/15, server typecheck 0
- 로그인 화면의 헤더·내비 모양은 브라우저로 확인 못 함 (자동화로 비밀번호 입력 안 함)

## 요청 5 (13:59)
> '잘못찍은 기록은 사장님께 수정을 요청하세요' -> 수정을 요청하고 받은 요청을 컨펌하는 부분이 없어. - 수정요청, 컨펌. - 휴가 및 대타근무에 대한 내용도 만들고 사장님이 직접 관리할 수 있도록 수정해줘. 위 내용도 스킬로 만들어서 관련 내용은 해당 스킬이 수정하고 업데이트된 부분은 자동으로 스킬도 업데이트 하도록 만들어.

### [배포] 14:01 디자인 셸
- API https://timesheet-api-hazel.vercel.app (usedByUserId), 웹 https://timesheet-brown-ten.vercel.app
- 확인: 웹 / /admin /login 200, /api/users/me 401, 번들에 "주요 메뉴"·아바타 색 포함

### [커밋] 14:10 PR #2 머지
- PR #1 은 사용자가 13:47 에 이미 머지(52fde73)해서 새 커밋이 반영되지 않았다 — close/reopen 시도는 "already merged" 로 거부(변경 없음)
- 두 커밋을 PR #2 로 열어 Vercel 체크 2건 pass, CLEAN → `gh pr merge 2 --merge` → 333c9e1
- [배포] main 머지로 웹 Git 연동 운영 배포가 자동으로 돌았다(timesheet-8lj4xiaij, Ready). 원격 빌드는 청크 경로가 /_next/static/immutable/ — 새 내비 코드 포함 확인
- 브랜치 feature/requests-leave 시작

### [결정] 14:17 휴가·대타의 급여 영향 — 공식 출처
- 고용노동부 빠른인터넷상담: "소정근로일 중의 지각, 조퇴, 휴일, 휴가, 휴업 등은 결근으로 처리할 수 없을 것", "주소정근로일 전부를 출근하지 않은 경우 ... 주휴 미지급해도 법위반으로 보기 어려움"
- → 승인된 휴가·대타(요청자)는 결근 아님. 개근 = 근무 ≥ 1일 AND 근무 + 결근 아닌 날 ≥ 소정근로일수. 유급 휴가는 1일 소정근로시간 × 시급
- PRD 08(수정 요청), 09(휴가·대타) 작성

### [검증] 14:17 pay 단위 테스트 21/21 (L-1~L-4, S-1 추가)
### [결정] 마이그레이션 requests_leave_substitution — 테이블 3개 추가만(DROP 0). 대기 수정 요청은 기록당 하나(부분 유니크 인덱스)

## 요청 6 (14:17)
> 로그인이 계속 유지되었으면 좋겠어. 로그인 인증관련해서 스킬만들어. 액세스토큰으로 쿠키에 담고 리프레시 토큰으로 유지하도록.

### [결정] 사용자에게 물어 정함
- 액세스 토큰: HttpOnly 쿠키로 옮긴다 → 모든 POST·PATCH·DELETE 에 CSRF(Origin + 토큰) 검사를 넓힌다
- 유지 기간: 슬라이딩 — 마지막 사용 후 30일 (기존 30일 절대 상한 제거)

### [결정] 14:23 요청 라우트 — server/src/requests/routes.ts
- 조건부 UPDATE(updateMany + where status) 로 상태 전이. 0건이면 없으면 404, 있으면 409 REQUEST_CLOSED
- 수정 요청 승인은 요청 상태 + 기록 변경을 한 트랜잭션. 기록이 사라졌으면 404 로 롤백, 요청은 대기로 남는다
- 대시보드에 absences·pendingRequests 추가

### [실패] 14:23 tsc — findMany 결과에 include 관계가 없다고 나옴
- 원인: `orderBy: [{ createdAt: "desc" }] as const` — 읽기 전용 튜플이 Prisma 인자 추론을 깨서 기본 반환 타입으로 떨어짐
- 수정: `{ createdAt: "desc" as const }` 객체로

### [결정] 14:23 인증 쿠키 전환 (서버)
- access_token 쿠키(HttpOnly, SameSite=Strict, Path=/api, 10분), 본문에 토큰 없음, Authorization 헤더 받지 않음
- 모든 POST·PUT·PATCH·DELETE 에 csrfGuard (app.ts). 라우트별 csrfGuard 제거
- 슬라이딩: 회전마다 session_expires_at = now + 30일, refresh TTL 30일

### [검증] 14:23 로컬 e2e 15/15 (S5 쿠키판, X-1 CSRF 전면, X-2 쿠키 발급·삭제, C-1~C-7, L·S·M), S17·S17b 2/2

### [결정] 14:33 웹 세션 쿠키판 — web/src/auth/session.ts
- 토큰을 JS 가 모른다. 부팅 = /users/me → 401 이면 refresh 1회. authFetch 가 변경 요청에 X-CSRF-Token 자동, CSRF 불일치면 재발급 1회
- 계약의 x-csrf-token 을 optional 로 표기 (서버는 필수 검사) — 필수면 생성 타입이 모든 호출에 헤더를 요구

### [실패] 14:33 Write 거부 — "File has been modified since read"
- 원인: 앞서 python 으로 고친 파일을 Write 로 덮으려 함. 내가 바꾼 것이라 다시 읽고 덮어씀

### [수정] 14:33 날짜 전용 문자열("2026-10-05")을 Date.parse 로 읽던 곳 → parseDay
- Date.parse 는 UTC 자정으로 읽어 서쪽 시간대에서 하루 앞 날짜가 보인다

### [결정] 14:33 스킬 — 프로젝트 timesheet-requests·timesheet-auth, 전역 web-auth references/cookie-session.md, kr-parttime-pay 2-1절(휴가·개근)
- CLAUDE.md 5절: 기능을 고치면 맡은 스킬을 같은 커밋에서 고친다

### [배포] 14:33 운영 DB 마이그레이션 requests_leave_substitution 적용
### [실패] 14:33 API·웹 배포 둘 다 `Error: fetch failed`
- 업로드 중 일시 오류(세 번째). 같은 순서(API → 웹)로 재시도 1회에 둘 다 READY
- 계약·인증이 바뀐 변경이라 API 와 웹 사이 공백을 짧게 두려고 연달아 올림

### [검증] 14:33 운영 e2e 8/8 (S3·S5·X-1·X-2·S14·I-1~I-8·C-1~C-7·L·S·M)
- 테스트 데이터 삭제: 사용자 16, 매장 6. 테스트 계정이 아닌 사용자 2·매장 1 은 남김

### [커밋] 14:33 7b8cb50 (코드), 72826c7 (문서·스킬) → feature/requests-leave, PR #3 https://github.com/knut15/timesheet/pull/3
- 내가 만들지 않은 미추적 파일(.agents/, .claude/agents/, .claude/skills/timesheet-team, .codex/, AGENTS.md, docs/team/)은 커밋에서 뺐다
- PR #3 은 머지하지 않았다 — 머지 승인은 PR 건마다 받는다

## 요청 7 (14:36)
> 유급 휴가로 신청 (사장님이 바꿀 수 있어요) 이건 멤버에서 제거하자 - 사장님이 알아서 판단하도록 수정

### [결정] 멤버 신청에 paid 없음. 신청은 paid=false 로 저장하되 "미정"으로 보이고, 마스터 승인 API 에서 paid 필수
- 스키마 컬럼은 그대로(nullable 로 바꾸면 파괴적 변경 아님이지만 기존 행 의미가 흔들린다) — 승인 전 값은 화면에서 쓰지 않는다

### [검증] 14:39 멤버 신청에서 유급 선택 제거
- 로컬 e2e 3/3 (L·S·M: 신청 응답 paid=null, paid 없이 승인 → 400 VALIDATION_FAILED, C-1, I-1)
- [배포] API → 웹. 웹이 `fetch failed` 1회 → 재시도로 READY
- 운영 e2e L·S·M 1/1, 테스트 데이터 삭제(사용자 4·매장 2, 실제 사용자 2·매장 1 유지)

### [실패] 14:39 배포 확인 grep 이 새 문구를 못 찾음
- 증상: 운영 번들에서 "승인하면서 정해요" 를 bash grep 으로 찾으면 0
- 원인: 최소화된 번들에서 한글이 \uXXXX 로 이스케이프돼 있다 (이 청크에서는)
- 수정: python 으로 원문·이스케이프 둘 다 검색 → 옛 문구 0, 새 문구 멤버·마스터 모두 있음

## 요청 8 (14:45)
> 근무 기록은 달력으로 보는 부분이 필요해. - 사장의 달력은 전체 멤버의 근무가 표시되어야 해. - 멤버의 달력은 본인의 근무 기록만 보이면 된다. 디자인 및 시니어 개발자 출동

### [결정] 사용자가 만든 팀 체계(docs/team, timesheet-team 스킬)로 진행 — TS-002 카드, PRD 10
- .claude/agents/timesheet-* 는 세션 시작 뒤 생겨 에이전트 유형으로 안 잡힘 → 범용 서브에이전트에 roles.md 절을 읽혀 위임
- 새 API 없음: 멤버 /shifts/me·/absences/me, 마스터 dashboard(월 ±8일 전 멤버 기록·absences)

### [커밋] 14:52 PR #3 머지 (c2a816c, 사용자 지시). 브랜치 feature/calendar 를 main 에서 시작
### [결정] 14:52 디자인 산출 — docs/design/calendar.md, timesheet-ui §4
- 마스터 내비: 대시보드·달력·요청·멤버·매장 (초대는 멤버 화면 안, /admin/invites 는 멤버 활성). 대가: 초대까지 한 번 더 누른다
- 대비 계산으로 오늘 칸 accent 채우기 안 함, 점선은 muted, 초록은 green-600. 시각 검증 미실시
### [실패] git switch main — "Your local changes ... would be overwritten"
- 원인: 로컬 main 이 머지 전(333c9e1)이라 전환하면 작업 중 문서가 덮인다. 뒤따르던 && 명령은 실행되지 않음(히스토리 한 줄만 먼저 들어감)
- 수정: git fetch 후 origin/main(트리가 현재와 같음, diff 0)에서 feature/calendar 를 땄다

### [검증] 15:02 시니어 산출 재검증 (부모)
- 변경 파일이 허용 목록 안인지 git status 로 확인. web test 35/35 (새 14), typecheck 0, lint 0, build — /admin/calendar
- 디자인 명세 §7 요일 예시가 하루씩 밀려 있던 것 12곳을 실제 요일로 수정 (2026-09-22 = 화)

### [배포] 15:02 웹 (서버 변경 없음) — fetch failed 1회 후 READY
### [실패] 15:02 배포 직후 /admin/calendar 404
- 원인: 배포가 READY 로 끝난 직후 운영 주소 연결이 바뀌기 전에 확인. 몇 초 뒤 다시 보니 200. vercel inspect 로 운영 주소가 새 배포(pvcy4gc9k)를 가리키는 것 확인
- TS-002 는 review — 실화면·독립 QA 미실시

## 요청 9 (15:04)
> 팀 파일도 커밋하고 머지해

### [커밋] 사용자가 만든 팀 체계 파일 — docs/team(TS-001·TS-002 카드 포함), .agents/skills/timesheet-team, .claude/agents, .claude/skills/timesheet-team(심볼릭 링크), .codex/agents, AGENTS.md
- 커밋 전에 내용을 읽고 비밀값 패턴(secret·token=·password·api key·private key) 검색 0건
### [커밋] PR #4 머지 (66d9bea) — 팀 파일 포함. main 머지로 웹 자동 배포 Ready, /admin/calendar 200
### [실패] 자동 배포 대기에 앞에 붙인 sleep 30 이 도구 규칙에 막힘 → until 루프로 상태를 기다림

## 요청 10 (15:14)
> 디자인팀 https://ui.shadcn.com/docs/components/base/avatar 이대로 timesheet 디자인 컴포넌트 가이드 만들고 개발팀은 mdx 로 페이지 만들어

### [결정] 사용자에게 물어 정함 — 문서 형식만 따른다(코드 그대로), MDX 설치, /guide 운영 공개
- 설치: @next/mdx 16.3.6, @mdx-js/loader 3.1.1, @mdx-js/react 3.1.1, @types/mdx 2.0.14
- TS-003 카드. 디자인(명세) ∥ 개발 1단계(MDX 기반), 이어 개발 2단계(페이지)

## 요청 11 (15:20)
> 캘린더 선택시 파란 라인 제거

### [수정] 달력 선택 칸 — border-2 border-accent 제거
- 선택 = bg-accent/10 + 날짜 font-bold (색만으로 구별하지 않는 규칙 때문에 굵기를 더함). 휴가 점선은 선택돼도 유지
- 키보드 focus-visible 바깥 선은 유지 (마우스·터치에는 안 보인다)
- docs/design/calendar.md 6곳 갱신. timesheet-ui §4 는 디자인 담당이 작업 중인 파일이라 그 담당에게 반영 요청
### [배포] 15:22 달력 선택 표시 수정 — 웹 READY, 운영 번들에 옛 border-2 0·새 규칙 1 확인
- 확인 스크립트 1회 404: 페이지가 가리키던 옛 청크를 요청. 청크별 예외 처리로 다시 확인
- 디자인 담당이 timesheet-ui §4 를 먼저 고쳐 내 편집의 전제 확인이 실패(중복 적용 없음). calendar.md 178행도 새 규칙으로

## 요청 12 (15:22)
> 사장은 로고 이미지(png, jpg, svg) 업로드 가능해야 하며 [Image #1] 빨간 표시된 곳에 로고가 노출되어야 한다
- 빨간 표시 = 헤더 위 작은 줄(eyebrow: "매머드 · 사장님", "매머드") — 마스터·멤버 모두

### [결정] 저장은 Postgres(stores 에 bytea 컬럼 추가, 1MB 이하) — 새 외부 저장소 없이
- SVG 는 스크립트를 담을 수 있다 → script·foreignObject·on* 속성·javascript:·외부 참조가 있으면 거부, 응답에 CSP sandbox·nosniff
- 파일 종류는 Content-Type 이 아니라 첫 바이트(PNG·JPEG 서명, SVG 는 <svg 시작)로 판정
### [검증] 15:25 로고 서버 — 로컬 e2e 6/6 (LG-1~LG-7 새로 + S3·X-1·I-1·C-1·L·S·M 회귀)
- Prisma 전역 omit 으로 logo 바이트는 기본 조회에서 빠진다 (/users/me 응답에 logo 없음 확인)

## 요청 13 (15:25)
> 초대코드를 멤버에게 발송해줘야 하는데, 문자로 발송했으면 좋겠어. / 아니면 초대코드 발송의 좋은 아이디어 찾아줘
### [결정] 사용자 선택 — 휴대폰 문자 앱으로(sms: 링크·공유). SMS API(업체 계정·발신번호 등록·요금)는 쓰지 않는다
### [결정] 15:29 로고 화면 — AppHeader logoUrl(eyebrow 앞 20px·최대 96px), 매장 화면 로고 카드(authFetch 로 파일 바이트 PUT)
### [결정] 15:29 초대 보내기 — "문자로 보내기": 번호(선택) → sms:<번호>?&body=, 공유 시트(navigator.share). 본문에 /join?code= 가입 링크
- /join 이 코드를 sessionStorage 에 두고, 온보딩이 읽어 채운다. 지우는 것은 등록 성공 뒤 — 렌더 중에 지우면 StrictMode 두 번째 렌더가 빈 값을 받는다
### [검증] 15:29 web test 39/39 (inviteLink 4 추가), typecheck·lint 0
### [배포] 15:29 운영 DB store_logo 마이그레이션(컬럼 추가만), API — fetch failed 1회 후 READY
### [검증] 15:29 운영 e2e LG-1~LG-7 1/1 (웹 주소 rewrites 경유)
### [검증] 15:36 가이드 2단계 재검증 (부모) — test 39/39, typecheck·lint 0, build ○ /guide 17
### [배포] 15:36 웹 — 가이드·로고 화면·초대 문자 (fetch failed 1회 후 READY). 운영 17쪽·/join·/admin/store·demo-logo.svg 200
### [실패] 15:36 브라우저 확인에서 두 가지
- 목차 옆 흰 세로 막대: 목차가 화면보다 7px 길어 스크롤바가 생겼고, color-scheme 선언이 없어 다크에서도 흰색으로 그려짐 → :root color-scheme: light dark + 목차 scrollbar-width: thin
- /guide/foundations 390px 에서 437px: 색 표의 sr-only(절대 위치)가 스크롤 틀(position 없음) 밖으로 나가 문서 폭을 넓힘 → 표 틀 4종에 relative
- 확인 방법: 창 크기 조절이 안 먹어서 운영 페이지 안에 390px iframe 을 만들어 17쪽의 scrollWidth 를 쟀다 → 0쪽 넘침
### [실패] 15:36 주석을 return ( 바로 뒤 JSX 로 넣어 파싱 에러 — 배포 반복문이 빌드 실패를 무시하고 3번 시도(산출물 없어 전부 실패, 배포된 것 없음)
- 수정: JS 주석으로 옮김. 배포는 이제 빌드 성공일 때만 한다
### [검증] 15:36 운영 테스트 데이터 삭제 — 사용자 2·매장 1 (실제 사용자 2·매장 1 유지)

## 요청 14 (15:44)
> 로고에 메머드 텍스트는 지워
### [수정] 로고가 있으면 매장 이름 글자를 빼고 로고가 이름을 대신한다
- AppHeader 에 logoAlt 추가: 이름은 로고 대체 텍스트로. 멤버 eyebrow 빈 값, 마스터 "사장님". 로고 없으면 그대로 이름
- PRD 11 LG-5, timesheet-ui 헤더 표, 가이드 app-header(구성·로고 절·API 표·예시) 같이 갱신
### [배포] 15:44 웹 READY — 운영 가이드 로고 예시: <img alt="데모 카페 성수점"> 뒤에 이름 글자 없이 제목이 온다
- 실제 매장 헤더(로그인 화면)는 브라우저로 보지 못함
### [커밋] PR #5 머지 (2aea702, 사용자 지시). main 자동 배포 Ready, /guide 200

## 요청 15 (15:54)
> [Image #2] 좌우 화살표 아이콘 심플하고 예쁜걸로 선택하고, 스크롤은 전부 https://ui.shadcn.com/docs/components/base/scroll-area 로 적용해줘

### [결정] 화살표 = lucide ChevronLeft/ChevronRight 를 IconButton(40px 원형, type="button")으로
- 디자인 담당이 지적했던 MonthPicker 의 type 누락(form 안에서 submit)도 같이 해결 → 가이드 "하지 말 것" 에서 그 줄 삭제

### [결정] shadcn ScrollArea(Base UI, base-nova) — 레지스트리 JSON(ui.shadcn.com/r/styles/base-nova/scroll-area.json)의 원본 그대로 components/ui/scroll-area.tsx
- 설치: @base-ui/react 1.8.0, clsx 2.1.1, tailwind-merge 3.7.0 (shadcn cn = twMerge(clsx) — autoqa 와 같은 구성). 사용자가 이 컴포넌트를 쓰라고 지시해서 그에 필요한 패키지로 설치
- 원본의 bg-border·ring-ring 을 우리 토큰에 잇는 테마 이름(--color-border: --line, --color-ring: --accent)만 globals 에 추가
- 대상: 앱 안에서 따로 스크롤되는 곳 10곳 — 전부 가이드(코드 블록 4종, 표 5곳, 목차). 앱 화면은 페이지 스크롤뿐이라 대상 없음
- 최대 높이는 Root 가 아니라 viewport 에 준다([&>[data-slot=scroll-area-viewport]]:max-h-…) — Root 에만 주면 viewport 가 늘어나 스크롤이 안 생긴다

### [배포] 15:54 웹 — fetch failed 1회 후 READY
### [검증] 15:54 운영 브라우저
- 달 이동 버튼 svg = lucide-chevron-left/right, 남은 기본 스크롤 요소 0, 가이드 17쪽 390px 문서 넘침 0, ScrollArea 안 가로 스크롤 62곳, 가로 스크롤바 썸 표시 확인
- web test 39/39, typecheck 0, lint 0 (ScrollBar 미사용 import 경고 1 → 제거)

## 요청 16 (15:59)
> 폰트는 pretendard로 수정
### [결정] npm pretendard 1.3.9 설치 → next/font/local 가변 폰트(--font-pretendard, 45~920). CDN 없음. 코드는 Geist Mono 유지, 안 쓰던 Geist Sans 제거
### [검증] 운영 /guide/foundations — body·h1 computed font = pretendard, 로드된 글꼴 pretendard·Geist Mono

## 요청 17 (15:59)
> 내가 지금 이런 규칙 정한걸 문서화해줘. 각 문서에 맞게
### [결정] 히스토리의 요청 1~16 을 규칙 단위로 뽑아 문서마다 있는지 대조, 빠진 곳을 채움
- timesheet-ui §0 디자인 기초(글꼴·화살표·스크롤·선택·로고) + shadcn 들이는 규칙 + 가이드 동기화 규칙
- 가이드 명세 §2-5, 웹 /guide/foundations "스크롤 영역·화살표"
- PRD 개요 공통 설정값: "근무자가 직접" → 마스터가 멤버별·매장별로 정함(로고·유급 여부 포함)
- docs/decisions.md — 규칙 → 적힌 곳 목록(링크만, 본문은 각 문서). CLAUDE.md §5 에 "새 규칙은 맞는 문서 먼저, 목록에 한 줄"
### [검증] 바뀐 문서 6개 상대 링크 깨짐 0, 금지어 0, build 성공, 운영 foundations 에 새 절

## 요청 18 (16:03)
> 모바일에서 멤버의 요청 페이지 보면 휴가신청의 시작/종료일 날짜 컴포넌트가 겹치고 대타요청의 날짜/동료선택의 높이가 다르며 겹친다.

### [실패] 모바일 요청 화면 — 날짜 두 칸 겹침, 날짜·선택 높이 차이
- 재현: 운영 가이드 Field 예시(같은 두 칸 격자)를 390px iframe 으로 — 높이 select 43 / input 46 재현. 겹침은 데스크톱 Chrome 에서 재현 안 됨(Chrome 날짜 칸은 줄어듦)
- 원인: .field 에 높이가 없어 요소마다 기본 높이가 달랐고, 격자 칸(label)에 min-w-0 이 없어 줄어들지 못했다. iOS Safari 날짜 칸은 기본 모양이 최소 폭·가운데 정렬이라 좁은 칸에서 옆을 덮는다(알려진 동작 — 실기기로는 확인 못 함)
- 수정: .field 에 h-11·min-w-0, 날짜 칸 appearance:none·왼쪽 정렬, select 는 appearance:none + lucide chevron-down 배경, Field 는 min-w-0 이고 안쪽 div→span(label 안 블록 요소 문제도 해결)
- 공용 스타일이라 마스터의 휴가·대타 등록, 멤버 조건 폼도 같이 고쳐짐
- 가이드 Field 선택 예시를 대타 요청과 같은 "날짜 + 선택" 두 칸으로, 명세·timesheet-ui §0 에 입력칸 규칙
### [검증] 16:03 운영 390·320px iframe — 모든 .field 높이 44 하나, 두 칸 행 2개 겹침 0(간격 12·8), 문서 넘침 0. select 셰브런 확대 확인
- iOS Safari 실기기 확인은 사람 확인 필요
### [커밋] PR #6 머지 (31c3e05, 사용자 지시). main 자동 배포 Ready, / · /guide 200

## 요청 19 (16:12)
> 멤버의 메인화면에서는 오늘 근무에 대한 대시보드가 필요하다. 디자이너 출동
### [결정] 디자인 1명 투입 → 부모가 시니어 역할로 구현. TS-004, PRD 12. 근무 일정 데이터가 없으므로 "예정" 은 보여 주지 않는다
### [결정] 16:13 위치 — 출퇴근 탭 시계 바로 아래 (사용자 "출퇴근 탭에서 시간 하단에 만들어줘"). 디자인 담당에게 전달, PRD 12·decisions 반영

## 요청 20 (16:18)
> 멤버 상단에 시간은 모바일에서 두줄로 나와(초단위가 개행처리됨) 한줄로 표시해줘
### [실패] 멤버 시계가 모바일에서 두 줄
- 원인: toLocaleTimeString("ko-KR") = "23시 59분 59초" 를 48px(text-5xl) Geist Mono 로 — 390px 에서 카드 안쪽 폭 293px 을 넘어 초가 다음 줄로
- 수정: lib/format.ts clockText() = HH:MM:SS(24시간, 항상 8자) + whitespace-nowrap. 디자인 담당(작업 중)에게 형식 변경 전달
### [검증] 16:18 web test 41/41(clockText 2 추가), typecheck·lint 0. 배포 READY("This operation was aborted" 1회 후 재시도)
- 운영 CSS·글꼴로 390px iframe 안에 멤버 화면 구조(px-5 + 카드 p-5)를 만들어 줄 수 측정 — 옛 표기 2줄, 새 표기 23:59:59·00:00:00 1줄·넘침 없음

### [구현] 16:25 요청 19 — 명세(docs/design/member-today.md) 대로
- lib/today.ts `todayState`(열린 기록 → 오늘 기록 → 오늘 휴가·대타 → 출근 전)·`todayShifts`·`toMinute` + 단위 테스트 8개
- ui.tsx `ProgressBar`, components/TodayDashboard.tsx `ClockCard`·`TodayDashboard` — API 를 부르지 않는 표시 컴포넌트(가이드 예시로 전 상태를 그리기 위해)
- ClockPanel: 옛 시계 카드 → ClockCard, 바로 아래 TodayDashboard. 기록·휴가를 읽기 전에는 출근 버튼을 막는다(옛 코드는 이미 출근한 사람에게 잠깐 `출근` 이 보였다)
- MemberHome: 출퇴근 탭을 고르거나 대시보드에서 급여 탭으로 갈 때 달 커서를 이번 달로 — 기록 탭에서 8월로 넘긴 뒤 돌아오면 8월 기록으로 오늘을 판정하던 문제
### [실패] 16:22 today.test.mjs 가 `Cannot find module .../src/lib/pay`
- 원인: today.ts 가 앱 규칙대로 "./pay" 를 확장자 없이 import — node 는 .ts 를 붙여 찾지 않는다
- 수정: calendar.test.mjs 와 같은 `registerHooks` resolve 훅 + 동적 import
### [검증] web test 49/49, typecheck·lint 0
### [결정] 가이드 페이지 3개(progress-bar·clock-card·today-dashboard, 예시 21개)는 개발 담당 1명에게 위임

## 요청 21 (16:24)
> 사장 어드민에서 각 멤버별 근무타임및 급여 설정 페이지 어딨어?
### [답] 따로 페이지 없음 — /admin/members 멤버 카드의 "조건 수정"(시급·주 시간·주 일수, `web/src/app/admin/members/page.tsx`). 요일별 근무 시간표는 데이터·화면 모두 없다(PRD 12 범위 밖)

### [검증] 16:40 개발 담당 — 가이드 3페이지(예시 21개), nav.ts, component-guide.md §1-3·§3·§4-16~18. typecheck·lint 0, build 34/34 정적. Basic 기대값(4시간 30분·14시간 / 20시간·진행 중 (4/5일)·144,480원)이 pay.ts 계산과 빌드 HTML 양쪽에서 명세와 같음
- 부모가 고침: 오늘 퇴근함 예시의 now 를 18:20:05 로(시계 14:32 인데 18:05 퇴근으로 보이던 것), 가이드 페이지 수 15→18
### [실패] 배포 직후 /guide/components/progress-bar 404
- 원인: 빌드 산출물에는 있었고(.vercel/output) 1~2분 뒤 200 — 배포 반영 전 요청. 코드 문제 아님
### [실패] 이번 달 금액이 `144,480` / `원` 두 줄 (가이드 390px 틀, 미리보기 안쪽이 좁다)
- 원인: 오른쪽 링크가 shrink-0 이라 왼쪽이 줄어들며 금액이 끊김
- 수정: 금액 whitespace-nowrap + 버튼 flex-wrap(좁으면 링크가 다음 줄). 명세 §3-4 에 한 줄
### [검증] 운영 가이드를 390·320px iframe 으로 — 세 페이지 모두 가로 넘침 0(scrollWidth 375/305·309), 시계 10개 전부 높이 48(한 줄), 알약 글자 상태별로 다름(출근 전·근무 중·오늘 퇴근함·오늘 휴가·오늘 대타·상태 확인 중…·상태를 불러오지 못했어요), 금액 6개 전부 한 줄(28px). 로그인 화면 실물은 자동화로 못 봄 — 사람 확인 필요

## 요청 22 (16:50)
> 다시 출근은 없어. 퇴근하면 내일 출근시간까지 버튼 비활성 처리하고 PR생성
### [결정] 근무 일정 데이터가 없어 "내일 출근시간" = 내일 0시. 오늘 퇴근(done)이면 출근 버튼 비활성 + 안내 한 줄, 50m 배너·알림도 이날은 끔(useGeofence clockedIn 에 포함 — 날짜가 바뀌면 알림 상태가 처음으로 돌아가 다시 울린다). PRD 12 T-7·TD-6, 명세 §2-3, 가이드, decisions, timesheet-ui §4-1
- 서버는 여전히 두 번째 출근을 받는다(다른 기기·API). 화면에서만 막음
### [검증] web test 50/50(TD-6 추가), typecheck·lint 0, 운영 배포 후 가이드 clock-card 에 안내 글자, `다시 출근` 버튼 0

## 요청 23 (16:52)
> 사장 페이지에서 각 멤버의 근무시간, 급여 설정은 어딨어?
### [답] 요청 21 과 같음 — /admin/members 멤버 카드 "조건 수정"
### [커밋] PR #7 생성 (99b3c6f·297b2f0) — https://github.com/knut15/timesheet/pull/7 . 머지는 사용자 지시 대기
